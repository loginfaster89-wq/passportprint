{
  "name": "studioprint-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend build pipeline for Studio Print — minifies and obfuscates HTML/JS before publishing",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^4.1.1"
  }
}
